/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package tema2;

import PaqueteLectura.Lector;

/**
 *
 * @author Martina
 */
public class PractEj4 {
    public static void main (String[] args){
        int dia = 5, turno = 8, i, j,diaDeseado, turnosTot = 0;
        String CORTE = "ZZZ", nombre;      
        int[] v = new int [dia];
        Persona[][] matriz = new Persona [dia][turno];
        System.out.println("Ingrese nombre ");
        nombre = Lector.leerString();
        for (i = 0; i < dia; i++){
            v[i] = 0;
        }
        while (turnosTot != (dia*turno) && !nombre.equals(CORTE)) {
            System.out.println ("Ingrese dia deseado");
            diaDeseado= Lector.leerInt()-1;
            if (v[diaDeseado] < turno){
                System.out.println ("Ingrese DNI y edad ");
                matriz[diaDeseado][v[diaDeseado]] = new Persona (nombre, Lector.leerInt(),Lector.leerInt());
                System.out.println("Ingrese nombre ");
                nombre = Lector.leerString();
                v[diaDeseado]++;
                turnosTot++;
            }
            else {
                System.out.println ("Ya no quedan turnos para dicho dia");
            }
        }
        for (i = 0; i < dia; i++) {
            System.out.println ("Dia " + (i + 1));
            for (j = 0; j < v[i]; j++) {
                System.out.println ("Para el turno " + (j + 1) + " la persona que se va a entrevistar es " + matriz[i][j].getNombre());
            }
        }
    }
}
//4- Sobre un nuevo programa, modifique el ejercicio anterior para considerar que:
//a) Durante el proceso de inscripción se pida a cada persona sus datos (nombre, DNI, edad) y el día en que se quiere presentar al casting. 
//La persona debe ser inscripta en ese día, en el siguiente turno disponible. En caso de no existir un turno en ese día, informe la situación.
//La inscripción finaliza al llegar una persona con nombre “ZZZ” o al cubrirse los 40 cupos de casting.
//Una vez finalizada la inscripción:
//b) Informar para cada día: la cantidad de inscriptos al casting ese día y el nombre de la persona a entrevistar en cada turno asignado.
